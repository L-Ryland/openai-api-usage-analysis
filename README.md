# OpenAI API Usage Analyze (Per User)

**Prerequisites**:

Install dependencies:

```shell
$ pip install -r requirements.txt
```

Copy `.env` to `.env.local` and fill in necessary data

```shell
$ cp .env .env.local
```

**Usage**:

Copy `.env` to `.env.local` and fill in necessary data

```shell
$ python analyze.py
```
